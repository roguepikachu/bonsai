// Package utils provides utility constants and functions for the Bonsai application.
package utils

// Common API path constants.
const (
	BasePath        = "/api/v1"          // BasePath is the root path for the API.
	HealthCheckPath = BasePath + "/ping" // HealthCheckPath is the endpoint for health checks.
)
